# Legacy Archive

`fsotres` is the source snapshot used for **Legacy Baseline**. It retains the
original parent directory and its two conflicting MATLAB code trees because
that conflict is part of the system's real history.

The archive is read-only by convention. Its role is reproducibility and code
forensics, not daily operation.
