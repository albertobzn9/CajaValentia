# Resultados

Carpeta local para guardar los CSV exportados desde esta copia autocontenida.

Cada sesion genera `nombre.csv` (diez columnas de ensayos) y
`nombre_palanqueos.csv` (eventos individuales de palancas). Los `.mat` que
quedan en `Valentia/` son estado interno temporal de MATLAB, no entregables.
